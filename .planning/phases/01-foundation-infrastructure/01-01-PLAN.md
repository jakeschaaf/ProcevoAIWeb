---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - tsconfig.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Next.js 16 project runs locally with npm run dev"
    - "Static export builds successfully with npm run build"
    - "Tailwind CSS v4 classes render correctly"
    - "Design system tokens (colors, typography, spacing) are defined"
    - "Vercel Analytics component is integrated in layout"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16 + Tailwind v4 + Vercel Analytics dependencies"
      contains: ["next", "tailwindcss", "@vercel/analytics"]
    - path: "next.config.ts"
      provides: "Static export configuration"
      contains: "output: 'export'"
    - path: "postcss.config.mjs"
      provides: "Tailwind v4 PostCSS plugin"
      contains: "@tailwindcss/postcss"
    - path: "app/globals.css"
      provides: "Design system with theme variables"
      contains: ["@import \"tailwindcss\"", "@theme"]
    - path: "app/layout.tsx"
      provides: "Root layout with Analytics"
      contains: ["Analytics", "@vercel/analytics"]
    - path: "app/page.tsx"
      provides: "Placeholder homepage with Tailwind classes"
      min_lines: 10
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "app/layout.tsx"
      to: "@vercel/analytics"
      via: "Analytics component"
      pattern: "<Analytics"
    - from: "postcss.config.mjs"
      to: "tailwindcss"
      via: "PostCSS plugin"
      pattern: "@tailwindcss/postcss"
---

<objective>
Create Next.js 16 project with TypeScript, static export, Tailwind CSS v4 design system, and Vercel Analytics integration.

Purpose: Establish the technical foundation that all future pages will build upon. A working local development environment with the design system ensures consistency from the first page onward.

Output: A Next.js project that builds successfully with static export, has a functioning design system with B2B-appropriate tokens, and includes Vercel Analytics ready for deployment.
</objective>

<execution_context>
@/Users/jakeschaaf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakeschaaf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 16 project with TypeScript</name>
  <files>
    package.json
    next.config.ts
    tsconfig.json
    app/layout.tsx
    app/page.tsx
    app/globals.css
  </files>
  <action>
Run `npx create-next-app@latest . --typescript --eslint --app --no-tailwind --no-src-dir` in the project directory to scaffold a Next.js 16 project.

After scaffolding, update `next.config.ts` to enable static export:
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  output: 'export',
  images: {
    unoptimized: true
  }
}

export default nextConfig
```

Verify the `tsconfig.json` includes path aliases:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

Run `npm run build` to verify static export works with the default page.
  </action>
  <verify>
`npm run dev` starts development server on localhost:3000
`npm run build` succeeds and creates `out/` directory with static HTML
  </verify>
  <done>
Next.js 16 project exists with TypeScript, App Router, and static export configured. Build produces static HTML in `out/` directory.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 design system</name>
  <files>
    package.json
    postcss.config.mjs
    app/globals.css
    app/page.tsx
  </files>
  <action>
Install Tailwind CSS v4 and PostCSS:
```bash
npm install tailwindcss @tailwindcss/postcss postcss
```

Create `postcss.config.mjs`:
```javascript
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
export default config;
```

Replace `app/globals.css` with the design system:
```css
@import "tailwindcss";

@theme {
  /* Typography - Professional B2B */
  --font-sans: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;

  /* Brand Colors - Monochrome + Blue Accent (B2B Standard) */
  --color-brand-50: oklch(97% 0.02 250);
  --color-brand-100: oklch(94% 0.04 250);
  --color-brand-500: oklch(55% 0.15 250);
  --color-brand-600: oklch(45% 0.13 250);
  --color-brand-900: oklch(20% 0.08 250);

  /* Neutral Gray Scale */
  --color-gray-50: oklch(98% 0 0);
  --color-gray-100: oklch(96% 0 0);
  --color-gray-200: oklch(90% 0 0);
  --color-gray-500: oklch(60% 0 0);
  --color-gray-700: oklch(40% 0 0);
  --color-gray-900: oklch(15% 0 0);
}

/* Base styles */
html {
  font-family: var(--font-sans);
}
```

Update `app/page.tsx` to use Tailwind classes to verify the design system works:
```tsx
export default function Home() {
  return (
    <main className="min-h-screen bg-gray-50 flex flex-col items-center justify-center p-8">
      <h1 className="text-4xl font-bold text-gray-900 mb-4">
        Procevo AI
      </h1>
      <p className="text-xl text-gray-700 mb-8 text-center max-w-2xl">
        AI-powered automation for mid-market companies ready to scale.
      </p>
      <button className="bg-brand-500 hover:bg-brand-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors">
        Get Started
      </button>
    </main>
  );
}
```

Run `npm run dev` and verify:
1. Background is light gray (gray-50)
2. Heading is dark (gray-900)
3. Button is blue (brand-500) and darkens on hover (brand-600)
  </action>
  <verify>
`npm run dev` shows styled page with gray background, dark text, and blue button
`npm run build` succeeds with Tailwind CSS processed
  </verify>
  <done>
Tailwind CSS v4 is installed with PostCSS, design system tokens are defined in globals.css, and homepage uses Tailwind classes correctly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Vercel Analytics integration</name>
  <files>
    package.json
    app/layout.tsx
  </files>
  <action>
Install Vercel Analytics:
```bash
npm install @vercel/analytics
```

Update `app/layout.tsx` to include the Analytics component:
```tsx
import type { Metadata } from 'next'
import { Analytics } from '@vercel/analytics/react'
import './globals.css'

export const metadata: Metadata = {
  title: 'Procevo AI',
  description: 'AI-powered automation for mid-market companies',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
```

Run `npm run build` to verify the build still succeeds with Analytics included.

Note: Analytics will only collect data when deployed to Vercel. Locally it renders nothing but doesn't break the build.
  </action>
  <verify>
`npm run build` succeeds
`grep -r "Analytics" app/layout.tsx` shows Analytics component is imported and rendered
  </verify>
  <done>
Vercel Analytics is installed and integrated into the root layout. Ready to track page views once deployed.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run dev` starts server and shows styled placeholder page
2. `npm run build` succeeds and creates `out/` directory
3. `ls out/` shows `index.html` and other static assets
4. `grep "tailwindcss" package.json` shows Tailwind v4 dependency
5. `grep "analytics" package.json` shows Vercel Analytics dependency
6. `grep "output.*export" next.config.ts` shows static export configured
</verification>

<success_criteria>
- Next.js 16 project runs locally (npm run dev works)
- Static export builds successfully (npm run build creates out/)
- Tailwind CSS v4 classes render (page shows styled content)
- Design system tokens defined (globals.css has @theme block with colors)
- Vercel Analytics integrated (layout.tsx includes Analytics component)
- Project is ready for Vercel deployment
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>
